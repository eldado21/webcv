export interface Formation {
    id: number;
    title: string;
    at: string;
    atUrl: string;
    latlng: [number, number];
    country: string;
    startDate: string;
    endDate: string;
    description: string;
}
