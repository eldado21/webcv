export interface Experience {
    id: number;
    title: string;
    at: string;
    atUrl: string;
    imageUrl: string;
    latlng: [number, number];
    country: string;
    startDate: string;
    endDate: string;
    description: string;
}
